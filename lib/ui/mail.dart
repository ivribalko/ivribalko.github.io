import 'package:flutter/material.dart';
import 'package:get/get.dart';

class Mail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('kDummyLarge'.tr),
      ),
    );
  }
}
